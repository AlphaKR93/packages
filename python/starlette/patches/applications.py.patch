--- work/sources/starlette/applications.py	2026-05-16 04:16:01.715524108 +0000
+++ work/patched/starlette/applications.py	2026-05-17 11:21:50.457098560 +0000
@@ -1,31 +1,33 @@
-from __future__ import annotations
+__all__ = ("Starlette",)
 
-from collections.abc import Awaitable, Callable, Mapping, Sequence
-from typing import Any, ParamSpec, TypeVar
-
-from starlette.datastructures import State, URLPath
-from starlette.middleware import Middleware, _MiddlewareFactory
-from starlette.middleware.errors import ServerErrorMiddleware
-from starlette.middleware.exceptions import ExceptionMiddleware
-from starlette.requests import Request
-from starlette.responses import Response
-from starlette.routing import BaseRoute, Router
-from starlette.types import ASGIApp, ExceptionHandler, Lifespan, Receive, Scope, Send
-
-AppType = TypeVar("AppType", bound="Starlette")
-P = ParamSpec("P")
+from .datastructures import State
+from .middleware import Middleware
+from .middleware.errors import ServerErrorMiddleware
+from .middleware.exceptions import ExceptionMiddleware
+from .routing import Router
+
+if __debug__ and __import__("typing").TYPE_CHECKING:
+    from collections.abc import Awaitable, Callable, Mapping, Sequence
+    from typing import Any, Self
+
+    from .datastructures import URLPath
+    from .middleware import _MiddlewareFactory
+    from .requests import Request
+    from .responses import Response
+    from .routing import BaseRoute
+    from .types import ASGIApp, ExceptionHandler, Lifespan, Receive, Scope, Send
 
 
 class Starlette:
     """Creates an Starlette application."""
 
     def __init__(
-        self: AppType,
+        self,
         debug: bool = False,
         routes: Sequence[BaseRoute] | None = None,
         middleware: Sequence[Middleware] | None = None,
         exception_handlers: Mapping[Any, ExceptionHandler] | None = None,
-        lifespan: Lifespan[AppType] | None = None,
+        lifespan: Lifespan[Self] | None = None,
     ) -> None:
         """Initializes the application.
 
@@ -54,7 +56,7 @@
         self.user_middleware = [] if middleware is None else list(middleware)
         self.middleware_stack: ASGIApp | None = None
 
-    def build_middleware_stack(self) -> ASGIApp:
+    def build_middleware_stack(self, /) -> ASGIApp:
         debug = self.debug
         error_handler = None
         exception_handlers: dict[Any, ExceptionHandler] = {}
@@ -77,42 +79,42 @@
         return app
 
     @property
-    def routes(self) -> list[BaseRoute]:
+    def routes(self, /) -> list[BaseRoute]:
         return self.router.routes
 
     def url_path_for(self, name: str, /, **path_params: Any) -> URLPath:
         return self.router.url_path_for(name, **path_params)
 
-    async def __call__(self, scope: Scope, receive: Receive, send: Send) -> None:
+    async def __call__(self, scope: Scope, receive: Receive, send: Send, /) -> None:
         scope["app"] = self
         if self.middleware_stack is None:
             self.middleware_stack = self.build_middleware_stack()
         await self.middleware_stack(scope, receive, send)
 
-    def mount(self, path: str, app: ASGIApp, name: str | None = None) -> None:
-        self.router.mount(path, app=app, name=name)  # pragma: no cover
+    def mount(self, path: str, app: ASGIApp, name: str | None = None, /) -> None:
+        self.router.mount(path, app, name)  # pragma: no cover
 
-    def host(self, host: str, app: ASGIApp, name: str | None = None) -> None:
-        self.router.host(host, app=app, name=name)  # pragma: no cover
+    def host(self, host: str, app: ASGIApp, name: str | None = None, /) -> None:
+        self.router.host(host, app, name)  # pragma: no cover
 
-    def add_middleware(self, middleware_class: _MiddlewareFactory[P], *args: P.args, **kwargs: P.kwargs) -> None:
+    def add_middleware[**P](self, middleware_class: _MiddlewareFactory[P], *args: P.args, **kwargs: P.kwargs) -> None:
         if self.middleware_stack is not None:  # pragma: no cover
             raise RuntimeError("Cannot add middleware after an application has started")
         self.user_middleware.insert(0, Middleware(middleware_class, *args, **kwargs))
 
     def add_exception_handler(
         self,
-        exc_class_or_status_code: int | type[Exception],
+        exc_class_or_status_code: int | type[BaseException],
         handler: ExceptionHandler,
+        /,
     ) -> None:  # pragma: no cover
         self.exception_handlers[exc_class_or_status_code] = handler
 
     def add_route(
         self,
         path: str,
-        route: Callable[[Request], Awaitable[Response] | Response],
-        methods: list[str] | None = None,
-        name: str | None = None,
-        include_in_schema: bool = True,
+        endpoint: Callable[[Request], Awaitable[Response] | Response],
+        /,
+        **kwargs,
     ) -> None:  # pragma: no cover
-        self.router.add_route(path, route, methods=methods, name=name, include_in_schema=include_in_schema)
+        self.router.add_route(path, endpoint, **kwargs)
