--- work/sources/starlette/middleware/exceptions.py
+++ work/patched/starlette/middleware/exceptions.py
@@ -1,19 +1,15 @@
-from __future__ import annotations
-
-from collections.abc import Mapping
-from typing import Any
-
-from starlette._exception_handler import (
-    ExceptionHandlers,
-    StatusHandlers,
-    wrap_app_handling_exceptions,
-)
-from starlette.exceptions import HTTPException, WebSocketException
-from starlette.requests import Request
-from starlette.responses import PlainTextResponse, Response
-from starlette.types import ASGIApp, ExceptionHandler, Receive, Scope, Send
-from starlette.websockets import WebSocket
+from .._exception_handler import wrap_app_handling_exceptions
+from ..exceptions import HTTPException, WebSocketException
+from ..requests import Request
+from ..responses import PlainTextResponse, Response
+from ..websockets import WebSocket
+
+if __debug__ and __import__("typing").TYPE_CHECKING:
+    from collections.abc import Mapping
+    from typing import Any
 
+    from .._exception_handler import ExceptionHandlers, StatusHandlers
+    from ..types import ASGIApp, ExceptionHandler, Receive, Scope, Send
 
 class ExceptionMiddleware:
     def __init__(
@@ -54,11 +50,7 @@
             self._status_handlers,
         )
 
-        conn: Request | WebSocket
-        if scope["type"] == "http":
-            conn = Request(scope, receive, send)
-        else:
-            conn = WebSocket(scope, receive, send)
+        conn: Request | WebSocket = Request(scope, receive, send) if scope["type"] == "http" else WebSocket(scope, receive, send)
 
         await wrap_app_handling_exceptions(self.app, conn)(scope, receive, send)
 
