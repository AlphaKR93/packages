--- work/sources/starlette/middleware/httpsredirect.py	2026-04-30 16:47:44.146039129 +0900
+++ work/patched/starlette/middleware/httpsredirect.py	2026-05-02 22:44:14.788164536 +0900
@@ -1,6 +1,8 @@
 from starlette.datastructures import URL
 from starlette.responses import RedirectResponse
-from starlette.types import ASGIApp, Receive, Scope, Send
+
+if __debug__ and __import__("typing").TYPE_CHECKING:
+    from starlette.types import ASGIApp, Receive, Scope, Send
 
 
 class HTTPSRedirectMiddleware:
@@ -8,7 +10,7 @@
         self.app = app
 
     async def __call__(self, scope: Scope, receive: Receive, send: Send) -> None:
-        if scope["type"] in ("http", "websocket") and scope["scheme"] in ("http", "ws"):
+        if scope["type"] == scope["scheme"] == "http":
             url = URL(scope=scope)
             redirect_scheme = {"http": "https", "ws": "wss"}[url.scheme]
             netloc = url.hostname if url.port in (80, 443) else url.netloc
