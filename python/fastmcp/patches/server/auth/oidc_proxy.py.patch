--- work/sources/fastmcp_slim/fastmcp/server/auth/oidc_proxy.py
+++ work/patched/fastmcp_slim/fastmcp/server/auth/oidc_proxy.py
@@ -13,6 +13,7 @@
 from typing import Literal
 
 import httpx
+import pydantic_core
 from key_value.aio.protocols import AsyncKeyValue
 from pydantic import AnyHttpUrl, BaseModel, model_validator
 from typing_extensions import Self
@@ -159,7 +160,7 @@
             response = httpx.get(str(config_url), **get_kwargs)
             response.raise_for_status()
 
-            config_data = response.json()
+            config_data = pydantic_core.from_json(response.content)
             if strict is not None:
                 config_data["strict"] = strict
 
