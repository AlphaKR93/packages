--- work/sources/fastmcp_slim/fastmcp/server/transforms/search/base.py
+++ work/patched/fastmcp_slim/fastmcp/server/transforms/search/base.py
@@ -26,16 +26,20 @@
     # The original tools are discoverable via search.
     mcp.add_transform(RegexSearchTransform())
 """
-
 from abc import abstractmethod
-from collections.abc import Awaitable, Callable, Sequence
-from typing import Annotated, Any
 
-from fastmcp.server.context import Context
-from fastmcp.server.transforms import GetToolNext
 from fastmcp.server.transforms.catalog import CatalogTransform
-from fastmcp.tools.base import Tool, ToolResult
-from fastmcp.utilities.versions import VersionSpec
+from fastmcp.tools import Tool
+
+
+if __debug__ and __import__("typing").TYPE_CHECKING:
+    from collections.abc import Awaitable, Callable, Sequence
+    from typing import Annotated, Any
+
+    from fastmcp.server.context import Context
+    from fastmcp.server.transforms import GetToolNext
+    from fastmcp.tools import Tool, ToolResult
+    from fastmcp.utilities.versions import VersionSpec
 
 
 def _extract_searchable_text(tool: Tool) -> str:
@@ -64,7 +68,7 @@
     ]
 
 
-SearchResultSerializer = Callable[[Sequence[Tool]], Any | Awaitable[Any]]
+type SearchResultSerializer = Callable[[Sequence[Tool]], Any | Awaitable[Any]]
 
 
 async def _invoke_serializer(
